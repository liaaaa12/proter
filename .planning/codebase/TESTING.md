# Testing

- PHPUnit configured locally for test runners inside `tests/` namespace.
- No specific Javascript testing pipeline (e.g. Jest or Playwright) has been implemented yet.
- Expected to run `php artisan test` for backend coverage verification.
